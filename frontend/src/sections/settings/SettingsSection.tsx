import React from 'react';
import Settings from '@/features/settings/pages/Settings';

// SettingsSection 将系统设置 feature 放入管理员 Minimal section 路由。
const SettingsSection: React.FC = () => <Settings />;

export default SettingsSection;
